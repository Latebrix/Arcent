package tech.arcent.settings

 /* TODO: restore when cloud avatar upload is re-enabled */

/*
import org.junit.Assert.assertEquals
import org.junit.Assert.assertNull
import org.junit.Test

class AvatarFunctionResponseParserTest {
    @Test
    fun parses_valid_file_id() {
        val body = "{\"success\":true,\"fileId\":\"abc123\"}"
        val id = AvatarFunctionResponseParser.parseFileId(body)
        assertEquals("abc123", id)
    }

    @Test
    fun returns_null_when_missing_file_id() {
        val body = "{\"success\":true}"
        assertNull(AvatarFunctionResponseParser.parseFileId(body))
    }

    @Test
    fun returns_null_for_blank_body() {
        assertNull(AvatarFunctionResponseParser.parseFileId("   "))
    }

    @Test
    fun returns_null_for_malformed_json() {
        val body = "not-json"
        assertNull(AvatarFunctionResponseParser.parseFileId(body))
    }
}
*/
